<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <vector
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:viewportWidth="500.0"
            android:viewportHeight="500.0"
            android:width="200dp"
            android:height="200dp">

            <path
                android:fillColor="@color/colorSettingIcon"
                android:pathData=" M  200   100  A  100 100 270 1 1 100 200"/>

        </vector>
        <shape
            android:shape="ring"
            android:innerRadiusRatio="2.5"
            android:thickness="4dp"
            android:useLevel="false"
            >
            <solid android:color="@color/colorSettingIcon"/>
        </shape>
    </item>
    <item android:id="@android:id/progress">
<!--        <rotate android:fromDegrees="360" android:toDegrees="360">-->
            <shape
                android:shape="ring"
                android:innerRadiusRatio="2.5"
                android:thickness="4dp"
                android:useLevel="true"
                >
                <solid android:color="@color/colorPrimary"/>
            </shape>
<!--        </rotate>-->
    </item>
</layer-list>
